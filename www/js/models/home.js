/*global m */
(function Home(n) {
    'use strict';
    
    // declaring controller
    n.model = function () {
    };
    
    // exporting
    window.listagram.home = n;
}(window.listagram.home || {}));